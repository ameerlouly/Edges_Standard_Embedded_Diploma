/*
 * q1.c
 *
 *  Created on: Jul 22, 2024
 *      Author: ameer
 */
#include <stdio.h>

int summation(int numbers_size, int* numbers)
{
	int sum = 0;
	for(int i = 0; i < numbers_size; i++)
		sum += numbers[i];
	return sum;
}
